export async function api<T>(path:string, init?:RequestInit){const u=`${process.env.NEXT_PUBLIC_API_URL||""}${path}`;const r=await fetch(u,{...init,cache:"no-store"});if(!r.ok)throw new Error(String(r.status));return r.json() as Promise<T>}
