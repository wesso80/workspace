'use client';
import { getSessionFromCookie } from "@/lib/auth";
import React,{Suspense} from 'react';
import SessionBadge from '@/components/SessionBadge';
import PortalButton from '@/components/PortalButton';
import DashboardInner from './DashboardInner';

export const dynamic = 'force-dynamic';

export default function DashboardPage(){
  return (
    <main className="container mx-auto px-4 py-8">
      <div className="mb-6 flex items-center gap-3">
        <h1 className="text-3xl font-bold">Dashboard</h1>
{tier === 'pro_trader' ? (
  <a href="https://app.marketscannerpros.app" target="_blank" rel="noopener noreferrer" className="inline-block mt-2 rounded bg-emerald-600 px-4 py-2 text-sm font-medium hover:bg-emerald-500">
    Open Pro Trader App
  </a>
) : (
  <a href="/pricing" className="inline-block mt-2 text-emerald-300 underline">
    View Pricing
  </a>
)}
        <SessionBadge/>
      </div>
      <Suspense fallback={null}><DashboardInner/></Suspense>
      <PortalButton/>
    </main>
  );
}
